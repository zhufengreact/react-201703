1. 先 clone 项目 `git clone git@github.com:llfly/react-201703.git`
2. 切到 group_1 分支 `git checkout group_1`
3. 更新最新代码 `git pull origin group_1`
4. 编辑修改 在group_1目录下对应的member
5. 提交修改 `git add .` `git commit -m 'xxx'`