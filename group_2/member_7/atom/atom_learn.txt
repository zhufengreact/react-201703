解决方案就很简单了，直接翻墙即可。

如果翻墙以后仍然不行，可以添加新的环境变量。我使用的方法是在Command中进行添加的：

以管理员身份运行Command Prompt

输入以下指令：

setx ATOM_NODE_URL http://gh-contractor-zcbenz.s3.amazonaws.com/atom-shell/dist /M

重启Atom，再次尝试安装Package

当然，新的URL也被墙了，所以同样需要翻墙。